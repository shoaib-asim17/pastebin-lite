# Pastebin Lite

A simple Pastebin-like application where users can create text pastes and share links to view them.
Pastes can optionally expire by time (TTL) or by number of views.

## Tech Stack
- Next.js (App Router)
- TypeScript
- Vercel KV (Redis) for persistence

## Running Locally

```bash
npm install
npm run dev
